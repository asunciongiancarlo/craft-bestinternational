<div class="slider__reel">

    {% for photo in photos %}
        <img src="{{ photo.getUrl('slider') }}" alt="{{ photo.title }}" class="slider__image" style="max-height: 600px">
    {% endfor %}

</div>